# makebimi
A free public REST API that validates a publicly reachable SVG file against the SVG Tiny Portable/Secure (P/S) profile used for BIMI logo files, checking XML/SVG structure and W3C SVG Tiny P/S (RFC 9345) requirements.
